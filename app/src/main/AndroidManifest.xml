<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.nmakadiya.expensemanager">

    <!--
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    -->
    <application
        android:name=".Firebase"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_foreground1"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_iconmain1_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".aboutus" />

        <activity
            android:name=".menu3"
            android:label="Charts"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".menu5"
            android:label="Report or Comparision By Category"
            android:theme="@style/AppTheme"></activity>

        <activity
            android:name=".menu4_report"
            android:label="Analysis or Comparision by Date"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".menu6_record"
            android:label="View/Delete Record"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:name=".Edit"
            android:label="Add Data"
            android:theme="@style/AppTheme">
            <!-- <intent-filter>
                 <action android:name="com.google.android.gms.actions.SEARCH_ACTION" />
                 <category android:name="android.intent.category.DEFAULT" />
             </intent-filter>-->
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="com.google.android.gms.actions.SEARCH_ACTION" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-2995472604015192~5326800643" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

    <dist:module dist:instant="true" />

</manifest>